@* How many days a propagation has been going, on the right of its row: "14 days". *@

<div class="day-stamp">
    <b>@Days</b>
    <small>@(Days == 1 ? "day" : "days")</small>
</div>

@code {
    [Parameter, EditorRequired]
    public int Days { get; set; }
}
